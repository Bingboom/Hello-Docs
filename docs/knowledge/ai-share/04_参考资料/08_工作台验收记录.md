# HTML 工作台：结果与核验边界

本页汇总 HTML 工作台已有的操作实录、导出文件和检查方法，并列出尚待验证的项目。

[回到完整练习](01_完整练习.html) · [工作台使用说明](07_工作台使用说明.html) · [下载同路线工具包](../downloads/xiaoye-poster-workbench.zip)

## 已留下什么结果

| 环节 | 已有材料 | 能确认什么 |
| --- | --- | --- |
| 三款排版 | [完整练习第 2 步](01_完整练习.html#s1) | 599、999、1599 元的三张海报，共享风格、不同布局 |
| 改价结果 | [工作台实录](../配图/HTML工作台-改价899-实录.png) 与 [899 元海报](../配图/小野500-改价899-实图.png) | 小野 500 显示 999 → 899、v2；另外两款界面显示 v1 |
| 自动导出 | [工作台实录](../配图/HTML工作台2-自动导出-实录.png)、[PDF](../07_HTML海报过程/自动导出/xiaoye-500-poster.pdf)、[PNG](../07_HTML海报过程/自动导出/xiaoye-500-poster.png) | 已留存 899 元导出文件；练习记录 PDF 为 A5 单页，含型号、价格与教学标记 |
| 重启 | [完整练习第 6 步](01_完整练习.html#s5) | 已记录重新启动后 899 元、v4 仍保留，下载入口可用 |
| 钉钉一次上传 | [完整练习第 7 步](01_完整练习.html#s6) | 三条教学记录及 PNG、PDF 上传和 MCP 回读实录；发现异常后重新上传 |

## 哪些结论还不能替它下

- 改价前的整页 HTML 工作台截图尚未记录；现有改价后截图不能代替完整的前后取证。
- 界面里另外两款仍为 v1，不等于已经提供了这两款改价前后的文件哈希报告。若要严格证明文件未变，需在同一轮测试中记录并对比。
- 第 3 步已经有资料表，独立运行统一生成入口后的逐款核对记录仍待补。
- 已有自动导出文件，不等于系统手动打印对话框也已经逐项验证。
- 工具说明要求导出失败时保留旧文件，但本页没有附上 HTML 版故障注入的完整记录，不能据此宣称所有失败路径都已通过。
- 包内有 macOS、Windows 启动脚本；现有记录不能替代每台电脑的环境检查与跨平台实测。
- 本地保存价格触发更新与钉钉自动同步不是一回事。后者尚未实现。

## 在自己的电脑上怎么验

第一次从零做海报，只需检查一张页面能打开，再改一处文字并核对。以下检查留到你决定运行完整工作台时再做：

1. 完整解压工具包，按使用说明检查环境，启动工作台。
2. 先把小野 500 设为 999，等输出成功。记录三款价格、版本与更新时间；需要严格核验时，让 Agent 记录三款 HTML、PDF、PNG 的文件哈希。
3. 只把小野 500 改为 899，点保存后不再发送生成指令。
4. 等成功后，分别打开新的 HTML、PDF、PNG，确认都是 899；再检查另外两款价格、版本、时间，必要时对比文件哈希。
5. 关闭服务并重新打开，确认资料和文件仍在。若失败，保留报错和截图，先排查，不把输入框的值当作完成依据。

不要为了贴合教程把版本号重置成截图中的数字，也不要在共享钉钉教学表中做破坏性测试。
